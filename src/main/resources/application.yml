spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/sag;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  mail:
    host: smtp.gmail.com
    port: 587
    username: jangeh303127@gmail.com
    password: yxqjgabpgczzugxg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret: "your-super-secret-key-that-should-be-at-least-32-characters-long-for-hs256"
  access-token-expiration: 3600000
  refresh-token-expiration: 604800000